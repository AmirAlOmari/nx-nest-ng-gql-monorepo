type User {
  _id: ID!
  email: String!
  firstName: String
  lastName: String
}

type Query {
  getAll: [User!]!
}

type Mutation {
  registerUser(input: RegisterUserInput!): User!
}

input RegisterUserInput {
  email: String!
  password: String!
  firstName: String
  lastName: String
}
